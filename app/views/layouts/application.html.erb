<!DOCTYPE html>
<html>
<head>

  <title>MillsFit</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  
</head>
<body>
      <div class="topnav">
        <b class="topnav b;" font-size="64px;" style=" font-family: Impact, Charcoal, sans-serif;"><%= link_to "MillsFit", root_path %></b>
          <% if current_googleuser2 %>
	      Signed in as <strong><%= current_googleuser2.name %></strong>!
              <a class="topnav a"><%= link_to 'Submit link', new_link_path %></a>
              <a class="topnav a"><%= link_to 'Account', edit_user_registration_path %></a>
              <a class="topnav a"><%= link_to "Sign out with Google", signout_path, id: "sign_out" %>
              <a class="topnav a"><%= link_to 'Sign out', destroy_user_session_path, :method => :delete %></a>
          <% else -%>
              <a class="topnav a"><%= link_to 'Submit link', new_link_path %></a>
              <a class="topnav a"><%= link_to 'Sign up', new_user_registration_path %></a>
              <a class="topnav a"><%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in" %></a>
          <% end -%>
      </div>

  <div id="main_content" class="container">
    <% flash.each do |name, msg| %>
      <%= content_tag(:div, msg, class: "alert alert-info") %>
    <% end %>

      <div id="content" class="col-md-9 center-block">
        <%= yield %>
      </div>
    </div>
  </div>
</body>
</html>
